name: Sync from upstream

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        branch: [main, staging, develop]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote add upstream https://github.com/orphic-inc/stellar-api.git

      - name: Sync ${{ matrix.branch }}
        run: |
          git fetch upstream
          # Reset branch to exactly match upstream — fork-specific changes
          # belong on feature branches, never on main/staging/develop.
          git checkout -B ${{ matrix.branch }} upstream/${{ matrix.branch }}
          git push origin ${{ matrix.branch }} --force
