# CLAUDE.md — stellar-api

The guidance for this repo lives in **[AGENTS.md](AGENTS.md)**, which is the single source of truth for every agent that works here — commands, commit workflow, environment, the architecture tree, established patterns, and testing.

This file exists only so Claude Code picks that up automatically. It is imported below; do not duplicate content here, or the two copies will drift apart (they did, and reconciling them is what created this split).

@AGENTS.md
